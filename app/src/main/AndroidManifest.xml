<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wypozyczalnia"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".user.SearchActivity" />
        <activity android:name=".user.CarsActivity"/>
        <activity android:name=".user.AccountActivity"/>
        <activity android:name=".user.CarActivity"/>
        <activity android:name=".user.HistoryActivity"/>
        <activity android:name=".admin.AdminCarsActivity"/>
        <activity android:name=".admin.AdminAddCarActivity"/>
        <activity android:name=".admin.AdminEditCarActivity"/>
        <activity android:name=".admin.AdminUsersActivity"/>
        <activity android:name=".admin.AdminUserActivity"/>
    </application>

</manifest>